
<div class="comments">
    {{#each comments}}
        <div class="row">
            <div class="col s12">
                <div class="card">
                    <div class="card-content">
                        <p>
                            {{this.comment}}
                        </p>
                            <p class="text-end"> -{{this.user.name}}, {{format_date this.date_created}}</p>
                    </div>
                </div>
            </div>
        </div>
    {{/each}}
</div>